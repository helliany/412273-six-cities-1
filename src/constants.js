const mapSettings = {
  zoom: 12,
  zoomControl: false,
  marker: true,
};

const CitiesNumber = {
  MIN: 0,
  MAX: 6,
};

const RATING_PERCENT = 20;

export {mapSettings, CitiesNumber, RATING_PERCENT};
